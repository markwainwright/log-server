{
  "name": "log-server",
  "description": "HTTP/HTTPS/TCP debug server that logs incoming requests in a human-readable way, and allows response behaviour to be customized",
  "version": "0.1.0",
  "author": "Mark Wainwright",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/markwainwright/log-server.git"
  },
  "type": "module",
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "check": "tsc --noEmit",
    "start": "node --import tsx src/index.ts",
    "start:watch": "node --watch --import tsx src/index.ts",
    "test": "node --test --import tsx test/*.test.ts test/**/*.test.ts",
    "test:watch": "node --test --watch --import tsx test/*.test.ts test/**/*.test.ts",
    "test:coverage": "c8 npm test"
  },
  "devDependencies": {
    "@tsconfig/node24": "^24.0.4",
    "@types/node": "^24.12.2",
    "c8": "^11.0.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  },
  "c8": {
    "100": true,
    "all": true,
    "include": [
      "src/**/*.ts"
    ],
    "exclude": [
      "src/index.ts"
    ],
    "check-coverage": true,
    "reporter": [
      "text",
      "html"
    ]
  }
}
